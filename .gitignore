node_modules
dist
.output
.nitro
.vinxi
.wrangler
.tanstack
.vercel
*.local
.env
.DS_Store
src/routeTree.gen.ts
dev.log

# Working material, not app source. Kept out of the repo on purpose:
# Screenshots/ and chat-images/ contain captures of a real myAT&T account
# (account numbers, balances, billing pages) and must not be published.
Screenshots/
chat-images/
First Document.docx
# Prompt dumps pasted from a signed-in att.com session: they carry the account
# number, customer id and live auth tokens along with the markup we wanted.
Prompt - *.txt

